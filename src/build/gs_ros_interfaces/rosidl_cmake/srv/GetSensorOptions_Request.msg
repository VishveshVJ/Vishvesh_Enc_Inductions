# Retrieve configuration for sensors on an entity
string entity           # Entity name or regex pattern
string[] names          # Optional: specific sensor names
string[] sensor_types   # Optional: specific sensor types (e.g. cam, lidar)
